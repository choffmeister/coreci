coreci {
  mongodb {
    host = "mongodb://localhost:27017"
    database = coreci_test
  }

  docker {
    workers = [
      {
        name = "localhost"
        host = "tcp://localhost:2376"
        tls = true
      }
    ]
  }
}
